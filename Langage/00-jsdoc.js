// @ts-check

/**
 * Additionne 2 paramètres
 * (commentaires jsdoc, http://usejsdoc.org/)
 * @param {number} a Le premier paramètre
 * @param {number} b  Le deuxième paramètre
 * @returns {number} La somme
 */
const add = (a, b) => a + b;

console.log(add(1, 2));
